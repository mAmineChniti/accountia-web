groups:
  - name: accountia_alerts
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: 'Instance {{ $labels.instance }} down'
          description: "{{ $labels.job }} a cessé de répondre depuis plus d'une minute."
